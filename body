In `crates/git-forge/src/exe.rs` L828–849, the directory branch of `resolve_path` can panic. When a directory exists on disk but has never been committed, `clean_oid` is `None` and `statuses` (with `include_untracked(false)`) is empty, so `dirty = false`. The subsequent `clean_oid.unwrap()` at L849 panics. The directory branch should return `Error::NotFound` when `clean_oid` is absent, matching the file branch's behaviour.\n\nGenerated-by: Claude Code (Claude Sonnet 4.6)