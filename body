**Prompt duplicates tool logic without sharing code**: `list_issues_prompt` and `get_issue_prompt` duplicate the same repo-open, store-create, list/get, serialize pattern from the tool handlers. The only difference is the return type wrapping. If the tool logic changes (e.g., to fix the error-handling issues noted elsewhere), the prompt must be updated in lockstep or they'll diverge. Extract the shared logic into helper methods on `ForgeMcpServer`.