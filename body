In `crates/forge-lsp/src/main.rs` L140`, `unwrap_or_default()` on `fs::read_to_string` produces an empty string when the draft file is unreadable. The subsequent `body.is_empty()` guard then silently drops the save — the user's in-progress comment is lost with no feedback. The read failure should be logged or surfaced as an LSP diagnostic.\n\nGenerated-by: Claude Code (Claude Sonnet 4.6)