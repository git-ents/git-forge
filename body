**`update_pull` does not guard against empty updates unlike `update_issue`**

`update_issue` (line 350-356) explicitly bails if all fields are `None`. `update_pull` has no such guard — it will happily send a PATCH request with an empty JSON body `{}`. This is an unnecessary API call and an inconsistency in the interface contract.