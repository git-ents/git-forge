In `crates/git-forge/src/contributor.rs` L194–200, `unwrap_or_default()` on a missing handle blob passes an empty string to `Handle::new("")`, which fails with a validation error. The failure message says "invalid handle" rather than indicating a corrupt contributor record. Return `Error::Config` or `Error::NotFound` with a meaningful message when the blob is absent.\n\nGenerated-by: Claude Code (Claude Sonnet 4.6)