In `crates/forge-mcp/src/review.rs` L126–176, `list_review_comments` collects threads via blob walk and separately collects threads anchored to the review OID. The `seen` set deduplicates by blob OID only. A comment thread discovered via both paths would appear twice in the output. The dedup should track thread IDs instead (or in addition).\n\nGenerated-by: Claude Code (Claude Sonnet 4.6)