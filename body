`comment new issue/N -b ""` succeeds, but the resulting comment cannot be found by `comment show -c <oid>`. Either reject empty bodies or handle them gracefully in display.

Assisted-by: Claude Code (Claude Sonnet 4.6)