In `crates/git-forge/src/review.rs` L500–503, `refresh_review_target` tries `source_ref` verbatim first, then falls back to `refs/heads/{sref}`. If a repo has both `refs/foo` and `refs/heads/foo`, the verbatim match wins silently rather than following standard git branch-name disambiguation rules. The fallback order should either match git conventions or be explicitly documented.\n\nGenerated-by: Claude Code (Claude Sonnet 4.6)