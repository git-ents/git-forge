In `crates/forge-mcp/src/issue.rs` and `crates/forge-mcp/src/review.rs`, every successful result path calls `.expect("serialize")` on `facet_json::to_string_pretty`. If serialization fails (e.g. a corrupt store entry), the MCP server process panics instead of returning `"error: …"` like every other error path. Replace with a match that returns a formatted error string.\n\nGenerated-by: Claude Code (Claude Sonnet 4.6)