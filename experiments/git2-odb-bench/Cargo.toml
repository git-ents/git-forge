[package]
name = "git2-odb-bench"
version = "0.0.0"
edition = "2024"
publish = false
license = "MIT OR Apache-2.0"
description = "Integration benchmarks proving Git as a build CAS."

[workspace]

[dependencies]
git2 = "0.19"
tempfile = "3"
rand = "0.8"
