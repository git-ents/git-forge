[package]
name = "gix-forge"
version = "0.1.0"
edition = "2024"

[dependencies]
gix = { version = "0.85.0", default-features = false, features = ["sha1", "zlib-rs"] }
thiserror = "2"

[dev-dependencies]
tempfile = "3"
test-support = { path = "../test-support" }
