[package]
name = "git-forge"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "git-forge"
path = "src/main.rs"

[[bin]]
name = "git-forge-lsp"
path = "src/bin/git-forge-lsp.rs"

[dependencies]
gix-forge = { path = "../gix-forge" }
gix = { version = "0.85.0", default-features = false, features = ["sha1", "zlib-rs", "interrupt", "revision"] }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
chrono = "0.4"
comfy-table = "7"
owo-colors = "4"
dialoguer = { version = "0.11", features = ["completion"] }
tower-lsp = "0.20"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "io-std"] }
serde_json = "1"
acdc-parser = { path = "../../vendor/acdc/acdc-parser", default-features = false, features = ["pre-spec-subs", "setext"] }
acdc-converters-core = { path = "../../vendor/acdc/converters/core", default-features = false }
acdc-converters-terminal = { path = "../../vendor/acdc/converters/terminal", default-features = false, features = ["pre-spec-subs", "highlighting"] }

[dev-dependencies]
proptest = "1"
tempfile = "3"
test-support = { path = "../test-support" }
